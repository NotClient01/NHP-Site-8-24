name: new-horizon-precision
static_sites:
  - name: web
    git:
      repo_clone_url: https://github.com/NotClient01/Site-Final.git
      branch: main
    source_dir: /
    routes:
      - path: /
    build_command: "npm install --no-fund --no-audit && npm run build"
    output_dir: dist
